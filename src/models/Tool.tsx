export interface Tool {
  toolName: string;
  description: string;
  id: string;
}
export interface CreateTool {
  toolName: string;
  description: string;
  createdAt: string;
  updatedAt: string;
}
